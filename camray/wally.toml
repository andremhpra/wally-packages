[package]
name = "andremhpra/camray"
version = "0.1.2"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
description = "Wrapper for raycasting from the player's camera to the player's mouse, supports InputObject input for mobile/console users."

[dependencies]
